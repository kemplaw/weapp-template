{
  "name": "weapp-template",
  "version": "1.0.0",
  "description": "weapp dev template",
  "main": "app.js",
  "scripts": {
    "lint": "eslint *.js --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kemplaw",
  "dependencies": {
    "miniprogram-computed": "^4.0.4"
  },
  "devDependencies": {
    "eslint": "^7.26.0"
  }
}
